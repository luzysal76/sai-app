# 작업팀 Report

**Topic:** 사이(Sai) 웹앱 챗봇 실제 AI 복구. 현재 상황: OpenAI 크레딧 소진(429), ANTHROPIC_API_KEY 미설정, 현재는 스마트 규칙 폴백으로만 동작 중. 목표: API 키 없이 또는 무료로 실제 AI 응답이 가능한 방법을 찾아 구현. 서버: D:/woman/web/server.js (Node.js, 포트 4321). 현재 server.js 3단계 폴백 구조: 1단계 OpenAI→2단계 Anthropic Claude→3단계 스마트규칙. 고려할 무료 AI 옵션: 1) Groq API (무료 티어, llama3/mixtral 모델, groq.com) 2) Together AI 무료 티어 3) Ollama 로컬 실행 (PC에 설치 가능 여부 확인) 4) Google Gemini API 무료 티어 5) Hugging Face Inference API 무료. 구현 방향: 무료 AI API 중 가장 좋은 것 선택 → server.js에 새 단계로 추가 → functions/.env에 환경변수 추가 → 테스트. 챗봇 역할: 연인 관계 전용 AI 상담사, 한국어 전용. 주의: Anthropic SDK는 이미 설치됨(functions/node_modules/@anthropic-ai/sdk), OpenAI SDK도 설치됨.
**Total Rounds:** 4
**Date:** 2026-06-17

---

## Round 1 — Stage 1: 계획 설계자 ✅

> **Passed → Stage 2**

### 현황 분석

**`functions/.env` 현재 상태:**
- `OPENAI_API_KEY` = 실제 키 존재 (크레딧 소진 → 429)
- `ANTHROPIC_API_KEY` = 플레이스홀더 (`sk-ant-여기에-실제-키-입력`) → `loadApiKey()`가 null 반환

**`web/server.js` 현재 구조 (299줄):**
- 1단계(line 246~259): OpenAI → 실패
- 2단계(line 261~281): Anthropic → 키 없어 실패
- 3단계(line 283~286): 스마트 규칙 → 현재 유일하게 작동

**결론**: 현재 모든 실사용자에게 스마트 규칙 폴백만 제공 중

---

### 선택 전략: **Groq API** (최우선 추천)

| 옵션 | 장점 | 단점 |
|------|------|------|
| **Groq** ⭐ | OpenAI SDK 재사용, 14,400회/일 무료, 속도 최빠름, 한국어 우수 | 가입 필요 |
| Gemini | 한국어 매우 우수 | 새 SDK 필요 |
| Ollama | 완전 로컬, 무료 | PC 사양·설치 부담 |
| HuggingFace | 완전 무료 | 느림, 안정성 낮음 |

**이유**: OpenAI SDK(`functions/node_modules/openai`)가 이미 설치돼 있고, Groq는 완전히 OpenAI 호환 API → **추가 설치 없이 `baseURL`만 바꿔서 즉시 연결 가능**

---

### 수정 파일 목록 및 변경 내용

#### 파일 1: `functions/.env` (2줄 추가)
```env
GROQ_API_KEY=gsk_여기에-Groq-키-입력
GROQ_MODEL=llama-3.1-8b-instant
```
→ 구현 단계에서 실제 Groq 키 발급 방법도 안내

#### 파일 2: `web/server.js` (3곳 수정, 약 20줄 추가)

**① `loadGroqKey()` 함수 추가** (line 36 이후, 약 5줄):
```js
function loadGroqKey() {
  const key = process.env.GROQ_API_KEY || '';
  return (!key || key.includes('여기에')) ? null : key;
}
```

**② Groq 시도 블록 추가** (현재 line 245 "1단계 OpenAI" 앞에 새 1단계로 삽입, 약 15줄):
```js
// ── 0단계: Groq (무료 LLaMA 3.1) ──────────────────────────────────────
const groqKey = loadGroqKey();
if (groqKey && OpenAI) {
  try {
    const groqModel = process.env.GROQ_MODEL || 'llama-3.1-8b-instant';
    const OpenAIClient = OpenAI.default || OpenAI;
    const client = new OpenAIClient({
      apiKey: groqKey,
      baseURL: 'https://api.groq.com/openai/v1'
    });
    const response = await client.chat.completions.create({
      model: groqModel, messages, temperature: 0.7
    });
    const content = response.choices[0].message.content || '';
    res.writeHead(200, { 'Content-Type': 'application/json' });
    return res.end(JSON.stringify({ content: content.trim(), source: 'groq' }));
  } catch (err) {
    console.warn('[chat] Groq 실패 →', err.message);
  }
}
```

**③ 기존 폴백 순서 유지** (OpenAI → Claude → 스마트규칙, 변경 없음)

---

### 최종 폴백 체인
```
1단계: Groq (무료, llama-3.1-8b-instant)  ← 새로 추가
2단계: OpenAI gpt-4o-mini                  ← 기존 (크레딧 충전 시 자동 활성화)
3단계: Anthropic Claude Haiku              ← 기존 (키 설정 시 자동 활성화)
4단계: 스마트 규칙 응답                    ← 기존 (항상 작동)
```

---

### 구현 단계 (2단계 담당자를 위한 지침)

1. **Groq 키 발급 방법 사용자 안내**:
   - `console.groq.com` 접속 → 무료 가입 → `API Keys` → `Create API key`
   - 형식: `gsk_xxxx...`

2. **`functions/.env`에 키 추가**:
   - `GROQ_API_KEY=gsk_발급받은키`
   - `GROQ_MODEL=llama-3.1-8b-instant`

3. **`web/server.js` 수정**:
   - `loadGroqKey()` 함수 삽입 (line ~36)
   - Groq 0단계 블록 삽입 (line ~245, 기존 OpenAI 블록 앞)

4. **서버 재시작 후 테스트**: `node web/server.js`

---

---

## Round 2 — Stage 2: 계획 검증자 ✅

> **Passed → Stage 3**

### 검증 결과 (실제 코드 대조)

**`functions/.env` 확인:**
- `ANTHROPIC_API_KEY` = `sk-ant-여기에-실제-키-입력` → `loadApiKey()` null 반환 ✅
- `OPENAI_API_KEY` = 실제 키 존재 (크레딧 소진) ✅
- `.env` 파서 정규식 `[A-Z_]+` → `GROQ_API_KEY`, `GROQ_MODEL` 모두 매칭 ✅

**`web/server.js` 구조 확인:**
- `loadOpenAIKey()` 종료 위치: **line 36** → `loadGroqKey()` 삽입 위치 정확 ✅
- OpenAI 1단계 블록 시작: **line 245** → Groq 블록 삽입 위치 정확 ✅
- OpenAI SDK 로드 변수명: `OpenAI` (line 47) → `if (groqKey && OpenAI)` 조건 정확 ✅
- `OpenAI.default || OpenAI` 패턴: 기존 코드(line 250)와 동일 → 재사용 가능 ✅

**엣지 케이스 검토:**
- `messages` 배열에 `system` role 포함 가능 → Groq는 OpenAI 호환이므로 그대로 전달 문제 없음 ✅
- `gsk_` prefix 키 → `key.includes('여기에')` 검사에서 false → 정상 통과 ✅
- `llama-3.1-8b-instant` → Groq 무료 티어 지원 모델 ✅
- Rate limit(`checkLimit`)은 Groq 단계에도 적용됨 (기존 `/api/chat` 핸들러 안에 포함) ✅

**사소한 불일치 (기능 영향 없음):**
- 계획 본문은 "새 1단계"로 표현, 코드 샘플은 `// ── 0단계: Groq`로 표기 → 주석 표기 차이만 있음, 동작에 영향 없음

---

---

## Round 3 — Stage 3: 구현자 ✅

> **Passed → Stage 4**

### 파일 읽기 → 수정 시작파일 구조 확인 완료. 이제 3가지 수정을 실행합니다.

**1. `functions/.env` — Groq 환경변수 추가****2. `web/server.js` — `loadGroqKey()` 함수 추가 (line 36 이후)****3. `web/server.js` — Groq 0단계 블록 삽입 (기존 OpenAI 1단계 앞)**수정 결과 최종 확인:구현이 정확하게 완료되었습니다. 최종 `.env` 상태도 확인:모든 구현이 완료되었습니다.

---

---

## Round 4 — Stage 4: 최종 검증자 🏆

### 검증 항목별 결과

#### `functions/.env` ✅
| 항목 | 상태 |
|------|------|
| `GROQ_API_KEY=gsk_여기에-Groq-키-입력` | ✅ 추가됨 (플레이스홀더 → 사용자 키 입력 대기) |
| `GROQ_MODEL=llama-3.1-8b-instant` | ✅ 추가됨 |
| `.env 파서 정규식` `[A-Z_]+` | ✅ `GROQ_API_KEY`, `GROQ_MODEL` 모두 파싱 가능 |

#### `web/server.js` ✅
| 항목 | 위치 | 상태 |
|------|------|------|
| `loadGroqKey()` 함수 | line 38-42 | ✅ `key.includes('여기에')` 플레이스홀더 검사 포함 |
| Groq 0단계 블록 | line 251-270 | ✅ 기존 OpenAI 1단계 앞에 정확히 삽입 |
| `baseURL: 'https://api.groq.com/openai/v1'` | line 259 | ✅ Groq 엔드포인트 정확 |
| `OpenAI.default || OpenAI` 패턴 | line 256 | ✅ 기존 코드와 동일 방식 |
| `source: 'groq'` 반환 | line 266 | ✅ 출처 추적 가능 |
| 에러 시 다음 단계 진행 | line 267-269 | ✅ catch에서 warn 후 폴백 |
| 파일 전체 크기 | 325줄 | ✅ 500줄 제한 이내 |

#### 폴백 체인 최종 순서 ✅
```
0단계: Groq (llama-3.1-8b-instant)  ← 신규 추가 (line 251)
1단계: OpenAI gpt-4o-mini            ← 기존 유지 (line 272)
2단계: Anthropic Claude Haiku        ← 기존 유지 (line 288)
3단계: 스마트 규칙 응답              ← 기존 유지 (line 310)
```

#### 엣지 케이스 검토 ✅
- `system` role 포함 messages → Groq는 OpenAI 완전 호환, 그대로 전달 가능 ✅
- `content` null 시 `|| ''` 처리 ✅
- `checkLimit()` Rate Limit은 Groq 포함 전체 `/api/chat` 에 적용 ✅

---

### 최종 보고서

#### 🟢 구현 완료 항목
1. **`loadGroqKey()` 함수** — `functions/.env`의 `GROQ_API_KEY`를 읽고 플레이스홀더 검사
2. **Groq 0단계 폴백** — OpenAI SDK 재사용, `baseURL`만 변경해 Groq 연결
3. **4단계 폴백 체인** — Groq → OpenAI → Claude → 스마트규칙 순서 완성
4. **환경변수 추가** — `GROQ_API_KEY`, `GROQ_MODEL` `.env`에 등록

#### 🟡 사용자 조치 필요
- **Groq API 키 발급 및 입력 필수**:
  1. `console.groq.com` 접속 → 무료 가입
  2. `API Keys` → `Create API key` → `gsk_xxx...` 형식 키 복사
  3. `functions/.env` 파일의 `gsk_여기에-Groq-키-입력` 부분을 실제 키로 교체
  4. 서버 재시작: `node web/server.js`

#### 🔵 추가 확인 필요
- **Ollama 로컬 실행** — 완전 무료, 인터넷 불필요이나 PC 설치 필요 (Groq 키 발급이 어려울 경우 대안)
- **Rate Limit 재검토** — 현재 Groq도 1일 5회 제한 적용 중. Groq 무료 티어는 14,400회/일이므로 제한 완화 여지 있음
- **`ANTHROPIC_API_KEY` 미설정** — 캡처 분석 기능이 여전히 비활성 상태 (별도 키 발급 필요)

---

---

